export enum SetupStoreId {
  App = 'app-store',
  Theme = 'theme-store',
  Auth = 'auth-store',
  Route = 'route-store',
  Tab = 'tab-store',
}

// 全局默认配置项
export enum globalConfig {
  HOME_URL = '/home',
  LOGIN_URL = '/login',
  ROUTER_WHITE_LIST = '/500,/404',
}
